document.addEventListener("DOMContentLoaded", () => {
    const ctaButton = document.getElementById("cta-btn");
    
    ctaButton.addEventListener("click", () => {
        alert("Zonke prototype interactive elements are working!");
    });
});